on:
  workflow_dispatch:
jobs:
  test_api:
    runs-on: ubuntu-latest
    steps:
      - name: set api key
        run: echo "api_key=123" >> $GITHUB_ENV
      - name: use api 
        run: echo " using api key $api_key"
